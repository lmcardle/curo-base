{
  "name": "curo-base",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run javascript && npm run static",
    "javascript": "npx rollup -c",
    "serve": "npm run build && npx mock-server",
    "static": "rsync -rm src/static/ dist"
  },
  "keywords": [],
  "author": "Liam McArdle",
  "dependencies": {},
  "devDependencies": {
    "@staticdeploy/mock-server": "^1.9.0",
    "rimraf": "^2.6.2",
    "rollup": "^0.67.4",
    "rollup-plugin-replace": "^2.1.0"
  }
}
